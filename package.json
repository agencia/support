{
  "name": "support",
  "version": "1.0.0",
  "description": "Support system to log support calls",
  "main": "app/index.html",
  "scripts": {
    "test": "jasmine test"
  },
  "author": "ulises.medrano@designamx.com",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0",
    "jasmine": "^2.5.2"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000 app/",
    "test": "jasmine"
  }
}
